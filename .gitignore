# Magento 2.4.7 – Git ignore
#
# NOTE: Your Magento application lives under `code/` in this repo.
# These rules scope ignores to that folder to avoid hiding Docker files, etc.

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*.orig

# Local env files
.env
.env.*
docker-compose.override.yml

# Magento credentials (keep samples tracked)
code/auth.json
code/auth.json.*
!code/auth.json.sample
!code/auth.json.dist

# Composer vendor (install in container)
code/vendor/
!code/vendor/.gitkeep

# Generated code
code/generated/*
!code/generated/.htaccess

# Caches, logs, reports
code/var/*
!code/var/.htaccess

# Static content (deployed or on-demand)
code/pub/static/*
!code/pub/static/.htaccess

# Media (runtime uploads). Keep the deny file.
code/pub/media/*
!code/pub/media/.htaccess

# Local error overrides
code/pub/errors/local.xml

# Frontend build artifacts
code/node_modules/
code/.grunt/

# PHPUnit / coverage / reports
coverage*
.phpunit.result.cache
phpunit.xml
phpunit.xml.dist

# Misc
*.log
*.tmp
*.cache
